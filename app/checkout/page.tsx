'use client'
import { useCart } from '@/components/cart'

export default function Checkout(){
 const {items,total}=useCart()
 return <main className="mx-auto grid max-w-5xl gap-12 px-5 py-14 md:grid-cols-[1fr_.7fr]"><div><p className="text-xs uppercase tracking-[.22em] text-primary">Checkout</p><h1 className="mt-4 text-5xl">Almost gathered.</h1><form className="mt-10 flex flex-col gap-5" onSubmit={e=>e.preventDefault()}><label className="flex flex-col gap-2 text-sm">Full name<input required className="border border-border bg-background p-3"/></label><label className="flex flex-col gap-2 text-sm">Phone<input required type="tel" className="border border-border bg-background p-3"/></label><label className="flex flex-col gap-2 text-sm">Email<input required type="email" className="border border-border bg-background p-3"/></label><label className="flex flex-col gap-2 text-sm">Delivery address<textarea required className="min-h-24 border border-border bg-background p-3"/></label><fieldset className="flex flex-col gap-3"><legend className="text-sm font-semibold">Payment method</legend><p className="border border-border bg-card p-3 text-sm">M-Pesa and Card <span className="float-right text-xs text-muted-foreground">Coming soon</span></p></fieldset><button disabled className="bg-muted px-6 py-4 text-sm font-semibold text-muted-foreground">Payment coming soon</button></form></div><aside className="h-fit bg-card p-6"><h2 className="font-serif text-2xl">Your order</h2><div className="mt-6 flex flex-col gap-4">{items.map(item=><div key={item.id} className="flex justify-between gap-4 text-sm"><span>{item.name} × {item.qty}</span><span>KSh {(item.priceKsh*item.qty).toLocaleString()}</span></div>)}</div><div className="mt-6 flex justify-between border-t border-border pt-4 font-semibold"><span>Subtotal</span><span>KSh {total.toLocaleString()}</span></div><div className="mt-2 flex justify-between text-sm"><span>Delivery</span><span>Free</span></div></aside></main>
}
