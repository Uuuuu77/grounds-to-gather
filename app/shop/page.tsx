import Link from 'next/link'
import Image from 'next/image'
const product={id:'highlands',name:'East Africa Highlands Roast',slug:'east-africa-highlands-roast',description:'Sourced from the highlands of East Africa, roasted in Nairobi. A cup that gathers your table and builds a future at its source.',weightG:250,priceKsh:1250,imageUrl:'/coffee-bag.png',stock:50}
export default function Shop(){return <main className="mx-auto max-w-6xl px-5 py-14"><p className="text-xs uppercase tracking-[.22em] text-primary">The collection</p><div className="mt-4 flex flex-col justify-between gap-4 md:flex-row md:items-end"><h1 className="text-5xl">Coffee to gather around.</h1><p className="max-w-xs text-sm leading-6 text-muted-foreground">One considered roast, sourced with care and roasted in Nairobi.</p></div><div className="mt-12 max-w-sm"><Link href={`/shop/${product.slug}`} className="group block"><div className="relative aspect-square overflow-hidden bg-muted"><Image src={product.imageUrl} alt={product.name} fill className="object-cover transition-transform duration-500 group-hover:scale-105"/></div><div className="mt-5 flex justify-between gap-4"><div><h2 className="font-serif text-2xl">{product.name}</h2><p className="mt-1 text-sm text-muted-foreground">{product.weightG}g · Nairobi roast</p></div><p className="font-semibold">KSh {product.priceKsh.toLocaleString()}</p></div></Link></div></main>}
