import Link from 'next/link'
import Image from 'next/image'
import { products } from '@/lib/products'
export default function Shop(){return <main className="mx-auto max-w-6xl px-5 py-14"><p className="text-xs uppercase tracking-[.22em] text-primary">The collection</p><div className="mt-4 flex flex-col justify-between gap-4 md:flex-row md:items-end"><h1 className="text-5xl">Coffee to gather around.</h1><p className="max-w-xs text-sm leading-6 text-muted-foreground">Two Murang&apos;a roasts, sourced with care and roasted in Nairobi.</p></div><div className="mt-12 grid gap-8 md:grid-cols-2">{products.map(p=><Link href={`/shop/${p.slug}`} key={p.id} className="group block"><div className="relative aspect-[4/3] overflow-hidden bg-muted"><Image src={p.imageUrl} alt={p.name} fill className="object-cover transition-transform duration-500 group-hover:scale-105" sizes="(max-width:768px) 100vw, 50vw"/></div><div className="mt-5 flex justify-between gap-4"><div><h2 className="font-serif text-2xl">{p.name}</h2><p className="mt-1 text-sm text-muted-foreground">{p.weightG}g · {p.roast} roast · {p.origin}</p></div><p className="font-semibold">KSh {p.priceKsh.toLocaleString()}</p></div></Link>)}</div></main>}
